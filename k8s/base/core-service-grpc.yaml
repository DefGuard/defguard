apiVersion: v1
kind: Service
metadata:
  annotations:
    traefik.ingress.kubernetes.io/service.serversscheme: h2c
  name: core-grpc
  labels:
    app: orion
    service: core
spec:
  ports:
    - name: grpc
      port: 50055
      targetPort: "grpc"
      protocol: TCP
  selector:
    app: orion
    service: core
