meta {
  name: List all bindings for location
  type: http
  seq: 1
}

get {
  url: {{base_url}}/{{api_prefix}}/network/:network_id/snat
  body: none
  auth: inherit
}

params:path {
  network_id: 1
}

headers {
  Authorization: Bearer {{api_token}}
}
