import type { BadgeProps } from '../../../shared/defguard-ui/components/Badge/types';

export const higherPlanBadgeProps: BadgeProps = {
  text: 'Available on higher-tier plans',
  icon: 'status-premium',
  showIcon: true,
  variant: 'warning',
};
