# Without running core on windows will result in stack overflow
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-args=/STACK:8388608"]

[env]
RUST_TEST_THREADS = { value = "1", force = true }
